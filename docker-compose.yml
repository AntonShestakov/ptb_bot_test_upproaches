services:
  bot:
    image: your-bot:${IMAGE_TAG:-latest}
    container_name: telegram-bot-prod
    restart: unless-stopped
    env_file: .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"